services:
  - type: web
    name: pharmaguard-backend
    runtime: python
    rootDir: pharmaguard-backend
    buildCommand: pip install -r requirements-deploy.txt
    startCommand: gunicorn -w 4 -b 0.0.0.0:$PORT app.main:app --timeout 120
    healthCheckPath: /api/v1/health
    autoDeploy: true